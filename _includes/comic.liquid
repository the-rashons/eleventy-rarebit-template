{%# If you want to remove one of the navigation buttons, you can do that here %}

{% render 'comicNav.liquid', collection: collection, page: page %}

<div id="comicPage">
  {% for panel in images %}
    <img src="{{ panel }}" alt="{{ alt }}">
  {% endfor %}
</div>

{% render 'comicNav.liquid', collection: collection, page: page %}